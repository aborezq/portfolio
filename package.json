{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling only. The deployed site is plain HTML/CSS/JS and ships with zero runtime dependencies.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
